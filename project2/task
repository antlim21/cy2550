First:
curl --remote-name https://prerelease.keybase.io/keybase_amd64.deb
Links to an external site.
Next:
sudo apt install ./keybase_amd64.deb
Then:
run_keybase
Once Keybase is open, login. Then in the terminal do:
keybase pgp select
